function calcularColor() {
    var color;
    if (num === 1) {
      color = "black";
    } else (num => 2);
      color = "white";
    if (num === 3) {
      color = "red";
    } else (color: "green");
      
    return "El color es " + color;
  }
  
  // código de prueba
  console.log(calcularColor(1)) // "El color es black"
  console.log(calcularColor(2)) // "El color es white"
  console.log(calcularColor(3)) // "El color es blue"
  console.log(calcularColor(8)) // "El color es green"
  